import { motion } from "framer-motion";

function Projects() {
  return (
    <motion.div>
      <motion.div className="grid grid-row-3"></motion.div>
    </motion.div>
  );
}

export default Projects;
